<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SR University | Home</title>

    <style>
        /* CSS EMBEDDED HERE */
        /* General Body Styles */
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            line-height: 1.6;
            color: #333;
            background-color: #f4f4f4;
        }

        .container {
            width: 80%;
            margin: auto;
            overflow: hidden; /* Clear floats */
        }

        /* Header & Navigation */
        header {
            background: #2c3e50; /* Dark blue */
            color: #fff;
            padding-top: 20px;
            min-height: 70px;
            border-bottom: #3498db 3px solid; /* Light blue accent */
        }

        header .logo {
            float: left;
            font-size: 2.5em;
            font-weight: bold;
            margin-top: -5px;
        }

        header .logo a {
            color: #fff;
            text-decoration: none;
        }

        header nav {
            float: right;
            margin-top: 10px;
        }

        header ul {
            margin: 0;
            padding: 0;
            list-style: none;
        }

        header li {
            display: inline;
            padding: 0 15px;
        }

        header a {
            color: #fff;
            text-decoration: none;
            text-transform: uppercase;
            font-size: 16px;
        }

        header a:hover,
        header a.active {
            color: #3498db; /* Light blue on hover/active */
            font-weight: bold;
        }

        /* Main Content Sections */
        main {
            padding-bottom: 50px; /* Space above footer */
        }

        section {
            padding: 40px 0;
            margin-bottom: 20px;
            background: #fff;
            border-bottom: 1px solid #ddd;
        }

        section:last-of-type {
            border-bottom: none;
        }

        h1, h2, h3 {
            color: #2c3e50;
            text-align: center;
            margin-bottom: 20px;
        }

        p {
            text-align: justify;
            margin-bottom: 15px;
        }

        /* Hero Section */
        #hero {
            min-height: 400px;
            background: #3498db; /* Light blue */
            color: #fff;
            text-align: center;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
        }

        #hero h1 {
            font-size: 3.5em;
            margin-bottom: 10px;
            color: #fff;
        }

        #hero p {
            font-size: 1.2em;
            margin-bottom: 30px;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        /* Buttons */
        .btn {
            display: inline-block;
            color: #fff;
            background: #e74c3c; /* Red */
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
            transition: background 0.3s ease;
        }

        .btn:hover {
            background: #c0392b; /* Darker red */
        }

        .btn-small {
            display: inline-block;
            color: #fff;
            background: #2c3e50; /* Dark blue */
            padding: 8px 15px;
            font-size: 0.9em;
            text-decoration: none;
            border-radius: 3px;
            transition: background 0.3s ease;
            margin-top: 15px;
        }

        .btn-small:hover {
            background: #3498db; /* Light blue */
        }

        /* Features Section */
        #features .feature-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin-top: 30px;
        }

        .feature-item {
            background: #ecf0f1; /* Light gray */
            padding: 20px;
            text-align: center;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }

        .feature-item h3 {
            color: #34495e; /* Medium dark blue */
            margin-top: 0;
        }

        /* Testimonials Section */
        #testimonials {
            background: #fff;
        }

        .testimonial-item {
            background: #f9f9f9;
            padding: 20px;
            margin-bottom: 20px;
            border-left: 5px solid #3498db;
            border-radius: 5px;
        }

        .testimonial-item p {
            font-style: italic;
            text-align: center;
            margin-bottom: 10px;
        }

        .testimonial-item cite {
            display: block;
            text-align: right;
            font-size: 0.9em;
            color: #666;
        }

        /* Footer */
        footer {
            background: #2c3e50;
            color: #fff;
            text-align: center;
            padding: 20px 0;
            position: relative;
            bottom: 0;
            width: 100%;
        }

        footer p {
            margin: 5px 0;
            font-size: 0.9em;
        }

        footer a {
            color: #3498db;
            text-decoration: none;
        }

        footer a:hover {
            text-decoration: underline;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            header .logo,
            header nav {
                float: none;
                text-align: center;
                margin-top: 10px;
            }

            header li {
                margin-bottom: 5px;
            }

            .container {
                width: 95%;
            }

            #hero h1 {
                font-size: 2.5em;
            }

            .feature-grid {
                grid-template-columns: 1fr; /* Single column on smaller screens */
            }
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <div class="logo">
                <a href="index.html">SR University</a>
            </div>
            <nav>
                <ul>
                    <li><a href="index.html" class="active">Home</a></li>
                    <li><a href="about.html">About Us</a></li>
                    <li><a href="services.html">Services</a></li>
                    <li><a href="contact.html">Contact</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main>
        <section id="hero">
            <div class="container">
                <h1>Welcome to SR University</h1>
                <p>Empowering minds through collaborative learning and skill development.</p>
                <a href="services.html" class="btn">Explore Our Programs</a>
            </div>
        </section>

        <section id="features">
            <div class="container">
                <h2>Why Choose SR University?</h2>
                <div class="feature-grid">
                    <div class="feature-item">
                        <h3>Expert Instructors</h3>
                        <p>Learn from experienced professionals passionate about teaching.</p>
                    </div>
                    <div class="feature-item">
                        <h3>Flexible Learning</h3>
                        <p>Choose from online or in-person classes to fit your schedule.</p>
                    </div>
                    <div class="feature-item">
                        <h3>Community Support</h3>
                        <p>Join a vibrant community of learners and grow together.</p>
                    </div>
                </div>
            </div>
        </section>

        <section id="testimonials">
            <div class="container">
                <h2>What Our Students Say</h2>
                <div class="testimonial-item">
                    <p>"SR University transformed my understanding of web development. Highly recommend!"</p>
                    <cite>- Alex P., Student</cite>
                </div>
                <div class="testimonial-item">
                    <p>"The support and resources provided by SR University are unparalleled."</p>
                    <cite>- Maria L., Professional</cite>
                </div>
            </div>
        </section>
    </main>

    <footer>
        <div class="container">
            <p>&copy; <span id="currentYear"></span> SR University. All rights reserved.</p>
            <p><a href="#">Privacy Policy</a> | <a href="#">Terms of Service</a></p>
        </div>
    </footer>

    <script>
        /* JAVASCRIPT EMBEDDED HERE */
        document.addEventListener('DOMContentLoaded', () => {
            // Set current year in footer
            const currentYearSpan = document.getElementById('currentYear');
            if (currentYearSpan) {
                currentYearSpan.textContent = new Date().getFullYear();
            }

            // Note: The contact form specific JS is removed as this is the Home page.
            // If you merge the contact page, you'd include that JS here.

            // Add 'active' class to current navigation link
            const navLinks = document.querySelectorAll('header nav ul li a');
            const currentPath = window.location.pathname.split('/').pop(); // Gets 'index.html', 'about.html', etc.

            navLinks.forEach(link => {
                const linkPath = link.getAttribute('href');
                if (linkPath === currentPath || (currentPath === '' && linkPath === 'index.html')) {
                    // For root path, index.html should be active.
                    // For other pages, direct match.
                    link.classList.add('active');
                } else {
                    link.classList.remove('active'); // Ensure others are not active
                }
            });
        });
    </script>
</body>
</html>